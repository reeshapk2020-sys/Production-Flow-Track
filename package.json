{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build --no-frozen-lockfile",
    "typecheck:libs": "echo 'Skipping typecheck on Vercel'",
    "typecheck": "echo 'Skipping typecheck on Vercel'"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}